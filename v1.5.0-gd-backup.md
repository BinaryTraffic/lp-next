# v1.5.0 GD バックアップ作業記録 — 2026-05-12

## この文書の目的

本セッションで実施した **GCP→Google Drive 同期** と **バグ修正** の記録。  
次回セッションの起点として使う。

---

## 現在の状態（2026-05-12 セッション終了時点）

```
GCP  ────────────────────────► Google Drive
      rsync 完了（GD と一致）
```

- **GCP が正**。GD は今回の rsync で GCP に追いついた。
- バックアップ: `lp_reverse_cms_v1.5.0-gd-backup_20260512.tar.gz`（313KB、GDルートに保存）
- バージョン: `APP_VERSION = 1.5.0`（`lp_reverse_cms/index.php` の定数）

---

## 今回のセッションで修正したファイル

### 1. `lib/LpAnalyzer.php` ★最重要（GCP にて修正、GD に反映済み）

#### バグ①: `bestPictureSourceSrcset()` — デスクトップ画像の上書き

**症状**: `<picture>` のスマホ専用 `<source media="(max-width:768px)" srcset="splp_ohp_01.jpg">` の値が、
デスクトップ向け `<img src="lp_ohp_01.jpg">` に上書きされていた。  
全セクションの背景画像がスマホ用小画像になりレイアウト崩壊。

**修正内容**: `max-width` のみを含む media query を持つ `<source>` をスキップ。

```php
if ($media !== '' && preg_match('/max-width/i', $media) && !preg_match('/min-width/i', $media)) {
    continue;
}
```

#### バグ②: `$extraCssContent` が `head_extra` に混入（前セッション修正）

**症状**: 外部 CSS ファイルのテキスト（385KB）が `<style>` タグなしで `<head>` に埋め込まれ、
ブラウザが body コンテンツとしてレンダリング。

**修正内容**: 外部 CSS は `$cssHaystack`（background-image 検出用）としてのみ使用し、
`head_extra`（生成 HTML に埋め込まれる）には含めないよう分離。

```php
$headExtra   = $this->extractHeadExtra($xpath);
$cssHaystack = $extraCssContent !== '' ? $headExtra . "\n" . $extraCssContent : $headExtra;
// extractSections には $cssHaystack を渡す（haystackのみ）
// lp_structure.json の head_extra には $headExtra のみ保存
```

`analyze()` の引数に `string $extraCssContent = ''` を追加済み。

---

### 2. `tools/analyze_worker.php`（GCP にて修正、GD に反映済み）

ダウンロード済み CSS ファイルを結合して `$extraCss` に格納し、background-image 検出精度を向上。

```php
$extraCss = '';
foreach (glob($cssDir . '/*.css') ?: [] as $cssFile) {
    $fsize = @filesize($cssFile);
    if ($fsize > 0 && $fsize < 600_000) {
        $content = @file_get_contents($cssFile);
        if ($content !== false) {
            $extraCss .= $content . "\n";
        }
    }
}
$structure = $analyzer->analyze($html, $finalUrl, null, 0.0, $extraCss);
```

---

### 3. `store/analyze_start.php`（GCP＆GD に修正済み）

**PHP_BINARY フォールバック**: mod_php 環境では `PHP_BINARY` が `.so` ファイルになるため CLI バイナリを明示解決。

**ログパス変更**: `/tmp/analyze_worker_debug.log`（固定・所有権問題の元凶）→
`/tmp/analyze_worker_{task_id}.log`（タスク単位）

```php
$phpBin = PHP_BINARY;
if (!is_file($phpBin) || !is_executable($phpBin)) {
    $phpBin = trim((string) shell_exec('which php8.2 2>/dev/null'))
           ?: trim((string) shell_exec('which php 2>/dev/null'))
           ?: '/usr/bin/php8.2';
}
// ...
. ' > /tmp/analyze_worker_' . preg_replace('/[^a-zA-Z0-9_\-]/', '', (string) $created['task_id']) . '.log 2>&1 &';
```

---

### 4. `store/generate_start.php`（GD に修正済み、GCP にはまだ未適用）

ログパス変更: `/tmp/generate_worker_debug.log` → `/tmp/generate_worker_{task_id}.log`

---

### 5. `deploy.sh`（GD に修正済み、GCP にはまだ未適用）

rsync オプションを drvfs（Windows H:ドライブ）対応に変更。

```bash
# 変更前
rsync -avz --delete \

# 変更後
rsync -rlz --delete --inplace \
  --exclude='*.tar.gz' \  # ← 追加
```

**理由**:
- `-a`（アーカイブ）に含まれるパーミッション変更・タイムスタンプ変更が drvfs では失敗する
- `mkstemp` が drvfs で使えないため `--inplace` が必要

---

## 次回セッションで最初にやること

### ✅ 必須: GD → GCP デプロイ（2ファイルの修正を本番反映）

```bash
# WSL から実行
bash /mnt/h/マイドライブ/projects/lp-next/deploy.sh
```

これにより以下が GCP に反映される:
- `store/generate_start.php` のログパス修正
- `deploy.sh` 自体の rsync オプション修正

> **確認**: `store/analyze_start.php` と `lib/LpAnalyzer.php` と `tools/analyze_worker.php` は
> 今回 rsync で GD に落としたもの（GCP 発）なので、再デプロイしても同一内容が戻るだけ。

---

### 🔲 推奨: git コミット

GCP 側が 167 ファイル分 HEAD より新しい状態のまま。コミットすることで履歴が残る。

```bash
# GCP にて
cd /home/lp-next/current/lp_reverse_cms
git add -A
git commit -m "v1.5.0: fix bestPictureSourceSrcset mobile override, cssHaystack separation, worker log per task_id"
git push origin main
```

> **注意**: `data/` `output/` は `.gitignore` 対象のため自動除外される。

---

### 🔲 任意: `/tmp` 古いログの掃除

前セッションのデバッグで残ったログファイルが `/tmp` にある可能性。

```bash
ls -la /tmp/analyze_worker_*.log /tmp/generate_worker_*.log 2>/dev/null
sudo rm /tmp/analyze_worker_debug.log 2>/dev/null || true
```

---

## GCP → GD rsync（逆方向・今後必要になったとき）

drvfs の制約により `-avz` ではなく `-rlz --inplace` を使う。

```bash
rsync -rlz --inplace \
  --exclude='.env' \
  --exclude='data/' \
  --exclude='output/' \
  --exclude='*.tar.gz' \
  gcp-lp:/home/lp-next/current/lp_reverse_cms/ \
  /mnt/h/マイドライブ/projects/lp-next/current/lp_reverse_cms/
```

> `-rlz` = recursive + symlinks preserve + compress。パーミッション/タイムスタンプは変更しない。  
> `--inplace` = mkstemp を使わず直接上書き（drvfs 必須）。  
> `--delete` は付けてよい（GD側の余分なファイルを削除）。

---

## インシデント記録（再発防止）

### `/tmp` ファイル所有権によるワーカー起動阻害（2026-05-12 解決済み）

- `shimizu` 所有の `/tmp/analyze_worker_debug.log` が存在すると、`www-data` がリダイレクト先に書き込めず、
  bash がコマンド自体を実行しない（タスクが 0/100 のまま止まる）。
- **解決**: `sudo rm /tmp/analyze_worker_debug.log`
- **恒久対策**: ログパスをタスク ID 単位に変更済み（今後は同名衝突が起きない）。

---

## ファイル場所まとめ

| ファイル | 説明 |
|---------|------|
| `lp_reverse_cms_v1.5.0-gd-backup_20260512.tar.gz` | GD バックアップ（rsync 前の状態） |
| `DEPLOY_NOTES_20260512.md` | 旧形式の作業メモ（本文書の前身） |
| `v1.5.0-gd-backup.md` | 本文書（次セッションへの引き継ぎ） |
| `deploy.sh` | GD→GCP デプロイスクリプト（drvfs 対応済み） |
