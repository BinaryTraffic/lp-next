# Browsable documentation directory — index.php works without AllowOverride Options
DirectoryIndex index.html index.php
# Fallback if both index files are missing (when AllowOverride permits)
Options +Indexes
