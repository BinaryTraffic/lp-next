# Browsable documentation directory
DirectoryIndex index.html
# Parent lp_reverse_cms/.htaccess sets Options -Indexes; enable listing here so
# /docs/ returns 200 (file list) when index.html is missing, instead of 403.
Options +Indexes
